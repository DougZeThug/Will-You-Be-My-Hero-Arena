# Verification: Watch

How to run this file: bring up the production page as the [README](README.md#how-to-run-a-pass) says (`pnpm build`, `PORT=3020 pnpm start`, `http://127.0.0.1:3020/`) and start every item from a fresh browser profile, or a fresh Playwright context, at 1440×1000 unless its Setup says otherwise. To return to a clean state between sections, close the profile, or clear `wybmh-paper-arena-v2`, `wybmh-input-bindings-v1` and the IndexedDB database `wybmh-character-library-v1`. Preferences must be at their defaults: in Arena settings, **Reduced motion** and **Lower graphics quality** unticked, the policy at win 3, draw 1, loss 0, **Entries / User** 4, **Counted entries enabled** ticked; Watch sound off; the clean spectator view off; no installed characters and no asset mappings. The setup dialog keeps its choices in memory until a reload, so reload before any item whose Setup says "default choices". A fresh save already holds the two counted basketball contests, so each demo user starts with 3 entries left and counted basketball offers **No entry remaining**. **Watch outcomes are decided at lock.** Never predict a score, winner, landing or duration. Read them from the result panel, from **The contest, as it happened**, from **Export immutable recording**, or from the newest entry of `recordings` in the save. The save, `localStorage["wybmh-paper-arena-v2"]`, is a JSON envelope whose `payload` is the save as JSON text. The fields these items use are:
- `recordings[]`, with `scores`, `winner` (0, 1 or null), `duration`, `unresolvedDraw`, `attempts[]` (`actor`, `contact`, `score`, `releaseAt`, `contactAt`, `commentary`) and `setup` (`seed`, `mode` `ranked` or `exhibition`, `tie`, `secret` for **Heat check**, `showcase`, `participants[]` with `userId`, `cardId`, `strategy`)
- `ledger`, the awards
- `policy`
- `active`, the contest waiting to resume, as `{id, time}` or null

The save carries a checksum, so read it but never hand-edit it, except where an item deliberately corrupts it. Items that need a particular outcome, such as a draw, extra pairs or a push, say to lock exhibitions until one occurs. **Replayable showcase seed** (`velvet-paw-29`, Cornhole only) repeats one contest exactly. Timing items need a machine with hardware graphics: under software rendering, playback ran several times slower than wall time. Use a screen recording and step through it, or read the times from the recording. What each Device value means here:
- **mouse**: clicking the page's own buttons.
- **keyboard**: Tab, Enter, Space and Escape on the page's own controls. Watch has no game keys.
- **hidden tab**: switching to another tab in the same window. This is not blur.
- **blur**: clicking another application's window so the browser loses focus while the Watch tab stays visible.
- **two tabs**: two tabs of the same profile, both on `/`, sharing one save.
- **storage**: developer tools. Use the Application panel for localStorage and IndexedDB, and the console where an item says so. A lost graphics context is made from the console with `(c=>(c.getContext('webgl2')||c.getContext('webgl')).getExtension('WEBGL_lose_context').loseContext())(document.querySelector('.pixi-host canvas'))`. A full save is made by filling localStorage with filler keys until writes throw: 1 MB strings, then 10 KB, then 100 bytes. Remove the filler keys afterwards.
- **small screen**: responsive mode at the width the item gives.

## watch/lobby.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LOBBY-01 | P1 | mouse | The page opens on the Watch lobby with **01 Cornhole** selected, titled "BACKYARD SPORTS / 01" over **Cornhole / HEAD TO HEAD** ([The simple case](../watch/lobby.md#the-simple-case)). | Fresh profile. | 1. Open `/`.<br>2. Read the header's tabs, the title and the event dock. | **Watch** is the active tab. The title reads "BACKYARD SPORTS / 01" and **CORNHOLE / HEAD TO HEAD**, and **01 Cornhole** is shown pressed. No progress bar, playback buttons or result panel. | pass (scripted pass 2026-09-24): the title read "BACKYARD SPORTS / 01 · CORNHOLE / HEAD TO HEAD". |
| LOBBY-02 | P2 | mouse | From top to bottom the lobby shows the title with the chip on the right, the event dock **CHOOSE YOUR EVENT** with **01 Cornhole**, **02 Football**, **03 Beer pong** and **04 Basketball**, the stage, the side station **ON THE CARD** with the duel cards **THROWS FIRST** and **THROWS SECOND**, a note, **Set up showdown** and a stamp, and the floor caption "GROSS POINTS. EQUAL ATTEMPTS. SETTLE IT ON THE COURT." with **House rules** ([Summary](../watch/lobby.md#summary)). | Fresh profile, 1440 px wide, no contest waiting. | 1. Read the page from the title down to the footer.<br>2. Click **House rules** in the floor caption. | Everything listed is present, in that order, with the side station to the right of the stage. Dan's card is **THROWS FIRST** and Doug's **THROWS SECOND**; the note reads "Choose your matchup. Every throw plays out automatically.". The dock has exactly four buttons. No resume banner. **House rules** opens the Cornhole rules. | — |
| LOBBY-03 | P2 | mouse | The stage previews the court with the two selected cards: the sign reads the event's name over **CHOOSE A MATCHUP**, and each nameplate reads **READY** with a score of 0 ([Summary](../watch/lobby.md#summary)). | Fresh profile. | 1. Wait for **UNFOLDING THE ARENA…** to clear.<br>2. Read the stage's hanging sign and both top-corner nameplates. | The sign reads **CORNHOLE** over **CHOOSE A MATCHUP**. The nameplates read **DAN** on the left and **DOUG** on the right, each 0 and **READY**. Both characters idle; nothing is thrown. | pass (scripted pass 2026-09-24): the sign read "CORNHOLE / CHOOSE A MATCHUP"; the nameplates read "DAN 0 READY" and "DOUG 0 READY". |
| LOBBY-04 | P2 | storage | The page's own label, "THE SAME CREW. HIGHER STAKES." and "{N} {units} each / Fully automatic", is hidden at every width, so only screen readers get it ([Summary](../watch/lobby.md#summary)). | Fresh profile. | 1. Look over the stage for "THE SAME CREW. HIGHER STAKES.".<br>2. In developer tools, find that element and read its rendered size.<br>3. Repeat at 900 px and 390 px wide. | The text is never visible. The element renders as a one-pixel box at every width, and it is still in the accessibility tree. | pass (scripted pass 2026-09-24): not visible; hit-testing its centre found the stage's drawing (document corrected after this pass). |
| LOBBY-05 | P3 | mouse | The stamp reads "{N} {UNITS} EACH / ~{S} SEC", where S is the estimated length in seconds ([Summary](../watch/lobby.md#summary)). | Fresh profile. | 1. Read the stamp under **Set up showdown**. | "4 BAGS EACH / ~31 SEC". | pass (scripted pass 2026-09-24): the stamp read "4 BAGS EACH / ~31 SEC". |
| LOBBY-06 | P1 | mouse | On a fresh save, the chip reads "{N} CLUB POINTS" and "Doug · Rank {n} · 3 entries left", where N is what Doug earned in the pre-played basketball fixture ([The simple case](../watch/lobby.md#the-simple-case)). | Fresh profile. | 1. Read the club points chip at the right of the title. | "3 CLUB POINTS" and "Doug · Rank 1 · 3 entries left". | pass (scripted pass 2026-09-24): "3 CLUB POINTS · Doug · Rank 1 · 3 entries left". |
| LOBBY-07 | P2 | mouse | Clicking **03 Beer pong** changes the title, reloads the stage with the beer-pong table, sets the sign to **BEER PONG** and updates the stamp ([The simple case](../watch/lobby.md#the-simple-case)). | Fresh profile, lobby on Cornhole, stage loaded. | 1. Click **03 Beer pong**.<br>2. Watch the stage until it settles, then read the stamp. | **03 Beer pong** is pressed, and the title reads "03" and **BEER PONG / HEAD TO HEAD**. The stage shows **UNFOLDING THE ARENA…**, then two tables with cup racks, and the sign reads **BEER PONG** over **CHOOSE A MATCHUP**. The stamp reads "6 BALLS EACH / ~44 SEC". | — |
| LOBBY-08 | P2 | mouse | **Set up showdown** opens the setup dialog for the selected event ([The simple case](../watch/lobby.md#the-simple-case)). | As LOBBY-07, on Beer pong. | 1. Click **Set up showdown**. | The dialog **Who’s stepping onto the court?** opens with the eyebrow **BEER PONG · 6 ATTEMPTS EACH**. | — |
| LOBBY-09 | P2 | mouse | Clicking either duel card also opens the setup dialog ([The interaction, event by event](../watch/lobby.md#the-interaction-event-by-event)). | Fresh profile. | 1. Click the **THROWS FIRST** card, then close the dialog with ×.<br>2. Click the **THROWS SECOND** card. | Each click opens the same setup dialog on the same choices. Closing it leaves the lobby unchanged. | — |
| LOBBY-10 | P2 | mouse | The lobby appears when the logo is clicked ([Starting](../watch/lobby.md#starting)). | Fresh profile, lobby on **02 Football**, then the **Standings** tab open. | 1. Click the logo at the top left. | The Watch lobby shows, still on **02 Football**. The address stays `/` and the page does not reload: the setup choices are kept. | — |
| LOBBY-11 | P1 | mouse | **Entries left** is the allowance minus the counted entries already locked, whether or not revealed ([Starting](../watch/lobby.md#starting)). | Fresh profile, default choices. | 1. **Set up showdown**, choose **Counted entry · points**, press **Start showdown**.<br>2. While the contest plays, read the chip. | The chip reads "2 entries left" once the lock finishes, while its points still read 3. | pass (scripted pass 2026-09-24): after the lock the chip read "3 CLUB POINTS" and "2 entries left". |
| LOBBY-12 | P2 | mouse | The chip follows whoever "you" are in the setup dialog, even if the dialog is closed without starting ([The club points chip](../watch/lobby.md#the-club-points-chip)). | Fresh profile. | 1. **Set up showdown**.<br>2. Set **Your demo user** to Sam.<br>3. Close the dialog with ×. | The chip reads "1 CLUB POINTS" and "Sam · Rank 2 · 3 entries left". No contest starts and nothing is saved. | — |
| LOBBY-13 | P1 | storage | **Set up showdown** stays disabled if the save cannot load; a corrupt save leaves it disabled with an error ([Starting](../watch/lobby.md#starting)). | Fresh profile. In developer tools, set `localStorage["wybmh-paper-arena-v2"]` to `{"payload":"{}","checksum":"bad"}`. | 1. Reload the page.<br>2. Read the error box under the stage, and try **Set up showdown**. | **Set up showdown** is disabled, and the error box shows a message with **Restore arena**. | pass (scripted pass 2026-09-24): "Character library: Error: The local save did not pass its integrity check.", and **Set up showdown** was disabled. |
| LOBBY-14 | P2 | mouse + storage | Choosing events, or opening and closing the setup dialog, saves nothing ([Backing out at once](../watch/lobby.md#backing-out-at-once)). | Fresh profile, developer tools open on localStorage. | 1. Click each of the four event buttons.<br>2. Open the setup dialog, change every choice, and close it.<br>3. Read localStorage. | No `wybmh-paper-arena-v2` key appears, and no other key changes. | pass (scripted pass 2026-09-24, page load only): no `wybmh-paper-arena-v2` key existed after opening the page. Event choice and the dialog were not checked. |
| LOBBY-15 | P1 | mouse | The selected event survives tab switches ([Backing out at once](../watch/lobby.md#backing-out-at-once)). | Fresh profile. | 1. Click **02 Football**.<br>2. Switch to **Standings**, then **The collection**, then back to **Watch**. | The lobby still shows **02 Football** pressed and the football title. | — |
| LOBBY-16 | P1 | mouse | The selected event does not survive a reload: the lobby reopens with Cornhole and default choices ([Backing out at once](../watch/lobby.md#backing-out-at-once); [Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile. | 1. Click **04 Basketball**.<br>2. **Set up showdown**, choose **Counted entry · points**, set **Your demo user** to Sam, and close.<br>3. Reload.<br>4. Open **Set up showdown**. | After the reload, **01 Cornhole** is selected and the chip shows Doug with 3 CLUB POINTS. The dialog opens on **Exhibition · no points**, Doug with Dan's card against Dan with Doug's. | — |
| LOBBY-17 | P2 | mouse | The lobby is replaced by playback when a recording loads, including by **Replay** from History ([Resolving](../watch/lobby.md#resolving)). | Fresh profile. | 1. Open the footer's **History**.<br>2. Press **Replay** on the basketball row. | The dialog closes and Watch shows **BASKETBALL / LIVE**. The event dock and **Set up showdown** are gone. | pass (scripted pass 2026-09-24): Replay from History switched to Watch and showed "BASKETBALL / LIVE". |
| LOBBY-18 | P1 | mouse | The chip's points and rank leave out a counted entry that is loaded but not complete, or waiting to resume ([The club points chip](../watch/lobby.md#the-club-points-chip)). | Fresh profile, default choices. | 1. Lock a counted entry.<br>2. During playback, read the chip.<br>3. Click the logo and read the chip again. | Both times the chip reads "3 CLUB POINTS" and "Doug · Rank 1", whatever the recording's outcome. | pass (scripted pass 2026-09-24): during playback the chip still read "3 CLUB POINTS". The lobby after the logo was not read. |
| LOBBY-19 | P2 | mouse | Clicking the chip opens that user's **Club member record** ([The club points chip](../watch/lobby.md#the-club-points-chip)). | Fresh profile. | 1. Click the chip. | The **Club member record** dialog opens for Doug, showing 3 points and one basketball contest. | — |
| LOBBY-20 | P2 | keyboard | The event buttons are ordinary buttons that show their pressed state to screen readers ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. Press Tab until **02 Football** has focus.<br>2. Press Enter.<br>3. Inspect the four buttons' `aria-pressed` in developer tools. | Football is selected as if clicked. `aria-pressed` is true on **02 Football** only. | — |
| LOBBY-21 | P2 | mouse | The selected event decides which counted entry the setup dialog offers ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. On Cornhole, **Set up showdown**, **Counted entry · points**, read the opponent, and close.<br>2. Click **04 Basketball** and repeat. | On Cornhole, **Scheduled opponent** Dan with **Start showdown** enabled. On Basketball, **No entry remaining** with **Start showdown** disabled. | — |
| LOBBY-22 | P2 | mouse | The duel cards show your card and the exhibition opponent card even when the setup dialog is on **Counted entry · points**, whose real opponent card is Doug ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. **Set up showdown**.<br>2. In **Exhibition · no points**, choose Dan's card for the opponent.<br>3. Switch to **Counted entry · points** and close the dialog. | The lobby's duel cards show Dan's card for **THROWS FIRST** and Dan's card for **THROWS SECOND**, not the counted opponent's Doug card. | — |
| LOBBY-23 | P2 | mouse | The duel cards and the stage preview use the cards chosen in the setup dialog, by default Dan's first and Doug's second ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. **Set up showdown** and choose Doug's card as your card.<br>2. Close the dialog with ×.<br>3. Wait for the stage. | The **THROWS FIRST** duel card becomes Doug's. The preview rebuilds, and its left nameplate reads **DOUG**. | — |
| LOBBY-24 | P2 | mouse | Reduced motion and lower graphics change the preview, and toggling rebuilds or restyles it at once ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile, lobby on Cornhole. | 1. Open Arena settings and tick **Lower graphics quality**.<br>2. Watch the stage behind the dialog; close it.<br>3. Reopen it and tick **Reduced motion**. | Ticking **Lower graphics quality** shows **UNFOLDING THE ARENA…** and rebuilds the preview. **Reduced motion** restyles it at once without a rebuild: the idles are calmer. | — |
| LOBBY-25 | P2 | mouse | The clean spectator view hides everything but the stage, which in the lobby leaves no way to set up a contest until it is turned off ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile; no contest waiting. | 1. Click the clean-view button at the bottom right of the stage.<br>2. Look for the title, the chip, the event dock, **Set up showdown** and the footer.<br>3. Click the button again. | The header, title, chip, event dock, side station with **Set up showdown**, floor caption and footer are hidden. The stage fills the width, with its sound and clean-view buttons. The second click restores everything. | — |
| LOBBY-26 | P3 | small screen | At 1000 px wide and below, **CHOOSE YOUR EVENT** is hidden ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. Set the width to 1001 px, then 1000 px, then 390 px. | The label shows at 1001 px and is hidden at 1000 px and 390 px. The four event buttons stay. | pass (scripted pass 2026-09-24, 390 px only): **CHOOSE YOUR EVENT** and **LOCAL DEMO** were hidden. |
| LOBBY-27 | P3 | small screen | At 720 px and below the side station moves under the stage ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. Set the width to 721 px, then 720 px. | At 721 px the side station is beside the stage. At 720 px it is below the stage. | — |
| LOBBY-28 | P3 | small screen | The stage keeps the court's 16:9 shape, so it is only about 208 px high in a 390 px window ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile. | 1. Set the width to 390 px.<br>2. Measure the stage's height in developer tools.<br>3. Repeat at 1440 px. | About 208 px high at 390 px, and about 620 px at 1440 px: the stage keeps 16:9. | pass (scripted pass 2026-09-24): 208 px at 390, 620 px at 1440 (document corrected after this pass). |
| LOBBY-29 | P2 | two tabs | Another tab's write updates the chip and the banner in place ([Modifiers](../watch/lobby.md#modifiers)). | Fresh profile, tabs A and B both on the lobby. | 1. In tab A, lock a counted entry and let it play a few seconds.<br>2. Look at tab B without reloading. | Within a second or two, tab B's chip reads "2 entries left" while its points stay 3. | pass (scripted pass 2026-09-24): tab B's chip went from "3 entries left" to "2 entries left" within 1.5 s; the points stayed hidden. |
| LOBBY-30 | P2 | keyboard | A dialog opened on top leaves the lobby underneath unchanged, and Escape or a click outside closes it ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile, lobby on **03 Beer pong**. | 1. Open Arena settings from the gear and look at the stage behind it.<br>2. Press Escape.<br>3. Open **House rules** from the floor caption and click outside it. | The preview keeps idling behind the dialog and does not rebuild. Each dialog closes. The lobby is still on Beer pong, the chip is unchanged, and nothing is saved. | — |
| LOBBY-31 | P2 | mouse | Clicking event buttons quickly reloads the stage for each, and only the last choice stays ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile. | 1. Click **02 Football**, **03 Beer pong** and **04 Basketball** within about 1 s. | The stage settles on the basketball court, the title reads **BASKETBALL**, and only **04 Basketball** is pressed. No error box. | — |
| LOBBY-32 | P2 | mouse | Navigating away keeps the selected event and setup choices in memory, and the stage is rebuilt on return ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile. | 1. **Set up showdown**, choose **Bold · wider swings**, close.<br>2. Switch to **Play**, then back to **Watch**.<br>3. Open **Set up showdown**. | On return the stage shows **UNFOLDING THE ARENA…** and rebuilds. The dialog still has **Bold · wider swings** selected. | — |
| LOBBY-33 | P2 | mouse | A policy change updates the entries left ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile. | 1. Arena settings: set **Entries / User** to 6 and press **Save for future entries**.<br>2. Close the dialog and read the chip. | The chip reads "5 entries left". The points and rank are unchanged. | — |
| LOBBY-34 | P2 | mouse | A reset updates the chip and removes the banner ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock a counted entry and click the logo during playback, so the banner shows and the chip reads "2 entries left". | 1. Arena settings, **Reset demo…**, **Reset demo data**.<br>2. Close the dialogs and read the lobby. | The chip returns to "3 CLUB POINTS" and "3 entries left". The resume banner is gone. | — |
| LOBBY-35 | P1 | storage | With nothing loaded, **Restore arena** clears the message and toggles **Lower graphics quality** ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)). | Corrupt save as in LOBBY-13, **Lower graphics quality** unticked. | 1. Reload.<br>2. Press **Restore arena**.<br>3. Open Arena settings. | Record what happens (suspected bug). The document says the message clears and **Lower graphics quality** is now ticked, with the stage rebuilt, although the player never asked for it. (B-13) | confirmed (scripted pass 2026-09-24): **Restore arena** cleared the message and switched **Lower graphics quality** from off to on. |
| LOBBY-36 | P2 | mouse | The lobby's event choice does not affect Play's, and Play's does not affect the lobby's ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). | Fresh profile. | 1. Click **04 Basketball**.<br>2. Switch to **Play** and choose **Backyard Brawl**.<br>3. Switch back to **Watch**. | Play setup showed its own default event, not Basketball. Back on Watch, **04 Basketball** is still selected. | — |
| LOBBY-37 | P2 | mouse | The stage's sound button is present in the lobby, but the preview makes no sound cues ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). | Fresh profile, speakers on. | 1. Press the speaker button at the bottom right of the stage ("Enable sound").<br>2. Listen for 30 s while the preview idles, and click an event button. | The button now reads "Mute sound", and the footer shows sound on. No tone, chime or thud is heard. | — |
| LOBBY-38 | P2 | storage | The duel card buttons are named "Change competitor 1" and "Change competitor 2", the stage's box "The backyard arena", and the chip by its text ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). | Fresh profile. | 1. Open the accessibility tree in developer tools.<br>2. Read the names of the duel cards, the stage box and the chip. | The names are as quoted, and the chip's name is its visible text. | — |
| LOBBY-39 | P2 | mouse | Installing a character selects it as your card, so it appears as the first duel card and in the preview ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). | Fresh profile and a valid `.arena-character.json` pack. | 1. The collection, **Install character**, install the pack.<br>2. Switch to **Watch**. | The **THROWS FIRST** duel card is the installed card, and the preview draws it on the left. | — |
| LOBBY-40 | P2 | two tabs | Each tab has its own selection, while the chip and banner follow the shared save ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). | Fresh profile, tabs A and B on the lobby. | 1. In tab A click **02 Football**.<br>2. Look at tab B. | Tab B stays on **01 Cornhole**. | — |
| LOBBY-41 | P2 | mouse | The bottom bar in the lobby shows placeholder text ([Edge cases](../watch/lobby.md#edge-cases)). | Fresh profile. | 1. Read the dark strip along the bottom of the stage. | **PEPPERONI CHEESERS** on the left and **HOME COURT ADVANTAGE: NONE** on the right. | pass (scripted pass 2026-09-24): "PEPPERONI CHEESERS / HOME COURT ADVANTAGE: NONE". |
| LOBBY-42 | P3 | mouse | The stamp's estimate is based on Dan's and Doug's timing, whatever cards are chosen ([Edge cases](../watch/lobby.md#edge-cases)). | Fresh profile, an installed character (see LOBBY-39), lobby on Cornhole. | 1. Read the stamp with the installed card as your card.<br>2. Switch your card back to Dan's in the setup dialog and read it again. | Both read "4 BAGS EACH / ~31 SEC". | — |

Not checkable by hand:

- Whether the clean spectator view should be available in the lobby at all, where it hides **Set up showdown** and the event dock, is a product call ([Open questions and verification](../watch/lobby.md#open-questions-and-verification); B-36).
- Whether the lobby's duel cards should show a counted entry's real opponent card and order is a product call ([Edge cases](../watch/lobby.md#edge-cases); see [the setup dialog](../watch/setup-dialog.md#open-questions-and-verification)).
- A preview that fails to load cannot be produced on demand. The document says it shows the error box ([Cancel and interrupt](../watch/lobby.md#cancel-and-interrupt)).
- `configure_arena_event` selecting a lobby event, switching to Watch and refusing while a recording is loaded needs a browser that ships WebMCP ([Interactions with other systems](../watch/lobby.md#interactions-with-other-systems)). The scripted pass of 2026-09-24 exercised it with an emulated `document.modelContext` and it passed; it belongs to the cross-cutting checklist.

## watch/setup-dialog.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SETUP-01 | P2 | mouse | The dialog's title is **Who’s stepping onto the court?**, and its eyebrow names the sport and attempt count, for example **CORNHOLE · 4 ATTEMPTS EACH** ([Summary](../watch/setup-dialog.md#summary)). | Fresh profile, lobby on Cornhole. | 1. Click **Set up showdown**. | The title and eyebrow read exactly as quoted. | — |
| SETUP-02 | P2 | mouse | The dialog cannot be opened while a recording is loaded: the duel cards are disabled, and **Set up showdown** is not shown ([Summary](../watch/setup-dialog.md#summary)). | Fresh profile, default choices. Lock an exhibition and let it play. | 1. Click each duel card in the side station.<br>2. Look for **Set up showdown**. | The clicks do nothing and no dialog opens. **Set up showdown** is nowhere on the page. | — |
| SETUP-03 | P2 | storage | The dialog cannot be opened while this browser's save has failed to load, because **Set up showdown** is disabled ([Summary](../watch/setup-dialog.md#summary)). | Corrupt save as in LOBBY-13. | 1. Reload.<br>2. Click **Set up showdown**.<br>3. Click each duel card. | No setup dialog opens by any route. | pass (scripted pass 2026-09-24, button only): **Set up showdown** was disabled. The duel cards were not tried. |
| SETUP-04 | P1 | mouse | On a fresh visit the dialog opens on **Exhibition · no points**, **Your demo user** Doug with Dan's card, **Opponent** Dan with Doug's card, **Steady · tighter grouping** and **Finish as a draw** ([The simple case](../watch/setup-dialog.md#the-simple-case)). | Fresh profile, lobby on Cornhole. | 1. Click **Set up showdown**.<br>2. Read every choice. | Exactly those choices. **Heat check · cosmetic stage effect** and **Replayable showcase seed** are shown, unticked. | pass (scripted pass 2026-09-24): all defaults as listed, with both cosmetic options shown for Cornhole. |
| SETUP-05 | P2 | mouse | The exhibition review strip reads "Exhibition. No leaderboard points. Unlimited matchups." and "Dan throws first · approximately N seconds" ([The simple case](../watch/setup-dialog.md#the-simple-case)). | As SETUP-04. | 1. Read the strip at the bottom of the dialog. | Both lines as quoted, with N = 31 for Cornhole. | pass (scripted pass 2026-09-24): "Exhibition. No leaderboard points. Unlimited matchups." and "Dan throws first · approximately 31 seconds". |
| SETUP-06 | P1 | mouse | **Start showdown** briefly reads **Locking the contest…**, the dialog closes, and the contest begins playing on the stage ([The simple case](../watch/setup-dialog.md#the-simple-case)). | As SETUP-04. | 1. Press **Start showdown** and watch the button.<br>2. Wait for the dialog to close and the stage to load. | The button reads **Locking the contest…** and is disabled. The dialog closes, the stage shows **UNFOLDING THE ARENA…**, and the entrances start by themselves: the playback button reads "Pause playback" and the bottom bar **● LIVE**. | pass (scripted pass 2026-09-24): locked in 0.63 s, and the contest autoplayed ("Pause playback", "● LIVE"). |
| SETUP-07 | P1 | mouse | In **Counted entry · points**, the opponent becomes **Scheduled opponent** with a name that cannot be changed, and the opponent's card becomes **LOCKED OPPONENT CARD**, always Doug ([The simple case](../watch/setup-dialog.md#the-simple-case)). | As SETUP-04. | 1. Click **Counted entry · points**.<br>2. Try to change the opponent and the opponent's card. | **Scheduled opponent** reads Dan as text, with no picker. **LOCKED OPPONENT CARD** shows Doug's card and cannot be changed. | pass (scripted pass 2026-09-24): **Scheduled opponent** Dan and **LOCKED OPPONENT CARD**. |
| SETUP-08 | P2 | mouse | The counted review strip lists the points: "Win +3 · Draw +1 · Loss +0. Both users receive their result once." ([The simple case](../watch/setup-dialog.md#the-simple-case)). | As SETUP-07. | 1. Read the review strip. | Exactly as quoted. | pass (scripted pass 2026-09-24): "Win +3 · Draw +1 · Loss +0. Both users receive their result once." |
| SETUP-09 | P2 | mouse | After the first time, the dialog opens on whatever was last chosen, including the mode tab ([Starting](../watch/setup-dialog.md#starting)). | Fresh profile. | 1. Open the dialog, choose **Counted entry · points**, **Bold · wider swings** and **Up to 3 extra equal pairs**.<br>2. Close it with ×.<br>3. Open it again. | It reopens on **Counted entry · points** with **Bold · wider swings** and **Up to 3 extra equal pairs** still chosen. | — |
| SETUP-10 | P2 | mouse | The sport is the lobby's, and the dialog offers no way to change it; the player must close the dialog and choose another event ([Starting](../watch/setup-dialog.md#starting); [Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile, lobby on **02 Football**. | 1. Open the dialog and look for any sport control.<br>2. Close it, click **04 Basketball**, open it again. | No sport control in the dialog. The eyebrow reads **FOOTBALL · 5 ATTEMPTS EACH**, then **BASKETBALL · 5 ATTEMPTS EACH**. | — |
| SETUP-11 | P2 | mouse | For a counted entry the dialog shows **N counted entries left** for your user ([Starting](../watch/setup-dialog.md#starting)). | Fresh profile. | 1. Open the dialog and choose **Counted entry · points**. | "3 counted entries left". | pass (scripted pass 2026-09-24): "3 counted entries left". |
| SETUP-12 | P2 | mouse | **N counted entries left** counts across all sports, so it can read "3 counted entries left" beside **No entry remaining** ([Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile, lobby on **04 Basketball**. | 1. Open the dialog and choose **Counted entry · points**. | **No entry remaining** beside "3 counted entries left", and **Start showdown** is disabled. | — |
| SETUP-13 | P2 | mouse + keyboard + storage | Closing the dialog with its ×, Escape or a click outside saves nothing ([Backing out at once](../watch/setup-dialog.md#backing-out-at-once)). | Fresh profile, developer tools open on localStorage. | 1. Open the dialog, change the choices, close it with ×.<br>2. Repeat, closing with Escape.<br>3. Repeat, closing with a click outside. | Each way closes the dialog. No `wybmh-paper-arena-v2` key is created, and no recording or award exists. | — |
| SETUP-14 | P2 | mouse | Closing does not revert the choices: they stay selected for next time, and the lobby's duel cards already show the chosen cards ([Backing out at once](../watch/setup-dialog.md#backing-out-at-once)). | Fresh profile. | 1. Open the dialog, choose Doug's card as yours and Dan's card for the opponent.<br>2. Close with Escape.<br>3. Look at the duel cards, then reopen the dialog. | Both duel cards now show Dan's and Doug's cards swapped: Doug's **THROWS FIRST**, Dan's **THROWS SECOND**. The dialog reopens with those cards chosen. | — |
| SETUP-15 | P1 | mouse + storage | **Start showdown** is disabled and reads **Locking the contest…** until the lock finishes, so a double-click locks one contest ([Committing](../watch/setup-dialog.md#committing)). | Fresh profile, default choices. | 1. Double-click **Start showdown**.<br>2. After the contest loads, count `recordings` in the save. | 3 recordings (the two basketball fixtures plus one). No error. | — |
| SETUP-16 | P2 | mouse + storage | An exhibition uses a fresh random seed, or `velvet-paw-29` if **Replayable showcase seed** is ticked for Cornhole ([Committing](../watch/setup-dialog.md#committing)). | Fresh profile, lobby on Cornhole. | 1. Lock an exhibition with the default choices; **Skip to result**; **Next showdown**.<br>2. Lock another the same way.<br>3. Lock two more with **Replayable showcase seed** ticked.<br>4. Read each recording's `setup.seed`, `scores` and attempts. | The first two seeds differ from each other and from `velvet-paw-29`. The last two have seed `velvet-paw-29` and identical attempts and scores. | — |
| SETUP-17 | P2 | storage | Your strategy applies to your card, and the opponent always plays Steady; the dialog does not say so ([Committing](../watch/setup-dialog.md#committing); [Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile. | 1. Open the dialog, choose **Bold · wider swings**, and look for an opponent strategy.<br>2. Lock the exhibition.<br>3. Read the new recording's `setup.participants`. | No opponent strategy control or note. Your participant has strategy `bold`; the opponent's is `steady`. | — |
| SETUP-18 | P1 | storage | For a counted entry your card is your choice, and the opponent's is Doug ([Committing](../watch/setup-dialog.md#committing)). | Fresh profile. | 1. Open the dialog, choose **Counted entry · points**, keep Dan's card as yours.<br>2. Lock it.<br>3. Read the new recording's `setup.mode` and participants. | `mode` is `ranked`. User Doug has card Dan; user Dan has card Doug. | — |
| SETUP-19 | P2 | storage | For a counted entry the schedule decides the users, their order and the seed ([Committing](../watch/setup-dialog.md#committing)). | Two fresh profiles, P and Q. | 1. In each, lock counted Cornhole with the default choices.<br>2. Compare the two new recordings' `setup.seed`, participant order and `scores`. | Identical seeds, order and scores in both profiles. | — |
| SETUP-20 | P2 | storage | For a counted entry, **Heat check** and the showcase seed are never applied, even if they were ticked in exhibition before switching ([Committing](../watch/setup-dialog.md#committing)). | Fresh profile, lobby on Cornhole. | 1. Open the dialog, tick **Heat check · cosmetic stage effect** and **Replayable showcase seed**.<br>2. Switch to **Counted entry · points** and lock.<br>3. Read the new recording's setup. | `secret` and `showcase` are false, and the seed is not `velvet-paw-29`. During round three the bottom bar never reads **HEAT CHECK / COSMETIC**. | — |
| SETUP-21 | P3 | mouse | Locking itself takes about 0.6 seconds ([While committed](../watch/setup-dialog.md#while-committed)). | Fresh profile, default choices. | 1. Press **Start showdown** and time **Locking the contest…**. | About 0.6 s. The dialog's closing and the stage's rebuild can take longer afterwards. | pass (scripted pass 2026-09-24): 0.63 s from **Start showdown** to the scoreboard existing. |
| SETUP-22 | P2 | storage | A refused lock leaves the dialog open with the reason in red under the button, the same reason in the error box, and nothing saved, for example when this browser's save refuses the write ([Resolving](../watch/setup-dialog.md#resolving); [Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile, default choices. Open the dialog, then fill localStorage to its quota (see "How to run this file"). | 1. Press **Start showdown**.<br>2. Read the dialog, then close it and read under the stage.<br>3. Remove the filler keys and read the save. | The dialog stays open and shows a red reason under the button. After closing it, the error box shows the same reason. No new recording is saved and nothing plays. | — |
| SETUP-23 | P2 | keyboard | Every control is reachable with Tab, and game keys do nothing ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile. | 1. Open the dialog with the keyboard: Tab to **Set up showdown**, Enter.<br>2. Tab through every control.<br>3. Press J, W and C.<br>4. Tab to **Start showdown** and press Enter. | Tab reaches the mode tabs, both users, every card button, the strategy, the tie rule, the checkboxes and **Start showdown**. The letter keys change nothing. Enter locks the contest. | — |
| SETUP-24 | P2 | mouse | The lobby's sport sets the attempt count, the estimate and whether **Replayable showcase seed** is offered: Cornhole only ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile, lobby on **02 Football**. | 1. Open the dialog. | The eyebrow reads **FOOTBALL · 5 ATTEMPTS EACH**, the strip "approximately 32 seconds". **Heat check** is offered, **Replayable showcase seed** is not. | — |
| SETUP-25 | P2 | mouse | Your collection lists every card your user owns: both built-in cards and every installed card; in exhibition the opponent's collection lists theirs ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile; then the same after installing one valid character pack. | 1. Open the dialog and count the cards under each user.<br>2. Install the pack from The collection and repeat. | Before: Dan's and Doug's cards on each side. After: those two plus the installed card on each side. | — |
| SETUP-26 | P3 | mouse | The dialog's short fade-and-zoom plays whatever the Reduced motion setting ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile, **Reduced motion** ticked in Arena settings, the operating system's reduced-motion preference off. | 1. Open and close the setup dialog. | It still fades and zooms in and out. | — |
| SETUP-27 | P3 | small screen | The dialog is at most the window width minus 36 px and at most 92% of the window height, and scrolls when its content is taller ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile, window 700×600. | 1. Open the dialog.<br>2. Measure it; scroll inside it. | It is at most 664 px wide and 552 px high. Its content scrolls inside it down to **Start showdown**. The page behind does not scroll sideways. | — |
| SETUP-28 | P2 | mouse | With **Counted entries enabled** off, the counted tab offers **No entry remaining** ([Modifiers](../watch/setup-dialog.md#modifiers)). | Fresh profile. | 1. Arena settings: untick **Counted entries enabled**, **Save for future entries**, close.<br>2. On Cornhole, open the dialog and choose **Counted entry · points**. | **No entry remaining**, and **Start showdown** is disabled. | — |
| SETUP-29 | P1 | keyboard | Closing the dialog during **Locking the contest…** lets the lock finish, but the contest then loads without playing by itself ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt); [Open questions and verification](../watch/setup-dialog.md#open-questions-and-verification)). | Fresh profile, default choices, on a slow machine or with CPU throttling in developer tools so that locking lasts long enough. | 1. Press **Start showdown** and, while it still reads **Locking the contest…**, press Escape.<br>2. Wait for the stage. | Record what happens (suspected bug). The document says the contest is locked and loaded but stays paused, offering **Resume playback**, until the player presses it. (B-35) | not reproduced (scripted pass 2026-09-24): the lock had finished in 0.6 s before Escape landed, and the contest autoplayed. |
| SETUP-30 | P2 | mouse | Choosing your user as the current opponent moves the opponent to the first other user, in the order Doug, Dan, Sam, Riley ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile, exhibition. | 1. Set **Your demo user** to Dan, the current opponent.<br>2. Set **Opponent** to Sam, then **Your demo user** to Sam. | After step 1 the opponent is Doug. After step 2 the opponent is Doug again: the first user other than Sam. | — |
| SETUP-31 | P2 | mouse | The main tabs and logo are covered, so the only way out is to close the dialog ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile. | 1. Open the dialog.<br>2. Click where **Standings** is in the header. | The click closes the dialog as a click outside, and the page stays on Watch; **Standings** does not open. | — |
| SETUP-32 | P2 | mouse | A reload forgets the choices ([Backing out at once](../watch/setup-dialog.md#backing-out-at-once); [Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile. | 1. Open the dialog, choose **Counted entry · points** and **Bold · wider swings**, close.<br>2. Reload and open the dialog. | The defaults of SETUP-04. | — |
| SETUP-33 | P2 | mouse | If the page goes away after the lock has finished, the lobby offers the contest through the resume banner ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile, default choices. | 1. Press **Start showdown**.<br>2. As soon as the dialog starts to close, reload. | The lobby shows "Saved at 0 seconds. Your result is waiting." with **Resume contest**. | — |
| SETUP-34 | P2 | two tabs | If another tab locks this counted pairing, the open dialog updates to **No entry remaining** and disables **Start showdown** ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile, tabs A and B on Cornhole. In B, open the dialog on **Counted entry · points**. | 1. In A, lock counted Cornhole with the default choices.<br>2. Look at B's open dialog without touching it. | Within a second or two, B's dialog shows **No entry remaining** and "2 counted entries left", and **Start showdown** is disabled. | pass (scripted pass 2026-09-24, partial): after A's lock, B's counted tab offered the next state. Whether B's dialog was already open was not recorded. |
| SETUP-35 | P2 | two tabs | If another tab changes the policy or resets the demo, the open dialog's entries left, review-strip points and availability follow ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)). | Fresh profile, tabs A and B. In B, open the dialog on **Counted entry · points**. | 1. In A, Arena settings: **Win** 5, **Save for future entries**.<br>2. Read B's review strip.<br>3. In A, lock counted Cornhole, then **Reset demo data**.<br>4. Read B's dialog. | After step 1, B's strip reads "Win +5 · …". After the reset, B's dialog offers **Scheduled opponent** Dan with "3 counted entries left" again, and the strip reads "Win +3 · …". | — |
| SETUP-36 | P1 | storage | **Start showdown** on a counted entry writes both users' awards immediately ([Interactions with other systems](../watch/setup-dialog.md#interactions-with-other-systems)). | Fresh profile, default choices. | 1. Choose **Counted entry · points** and press **Start showdown**.<br>2. While the entrances play, read `ledger`. | 6 awards instead of 4: one for Doug and one for Dan for the new contest, tagged `club-points-v1`, with the win and loss values read from the recording's winner. | pass (scripted pass 2026-09-24): the ledger went from 4 to 6 awards, Doug +3 win and Dan +0 loss, `policyId` `club-points-v1`. |
| SETUP-37 | P2 | mouse | Installing a character from The collection selects it as your card and turns off **Replayable showcase seed** ([Interactions with other systems](../watch/setup-dialog.md#interactions-with-other-systems)). | Fresh profile, a valid character pack. Open the dialog on Cornhole, tick **Replayable showcase seed**, close. | 1. Install the pack from The collection.<br>2. Back on Watch, open the dialog. | Your card is the installed card, and **Replayable showcase seed** is unticked. | — |
| SETUP-38 | P2 | storage | The dialog has a title and description and takes focus when it opens; the pickers are labelled, card choices are announced as "Select {card name}" with a pressed state, and an error has `role="alert"` ([Modifiers](../watch/setup-dialog.md#modifiers); [Interactions with other systems](../watch/setup-dialog.md#interactions-with-other-systems)). | Fresh profile, accessibility tree open in developer tools. | 1. Open the dialog.<br>2. Read the focused element, the dialog's name and description, and the card buttons.<br>3. Produce a refusal as in SETUP-22 and read the error's role. | Focus is inside the dialog. The card buttons are named "Select Dan Weidensaul" and "Select Doug Weidensaul", with `aria-pressed` true on the chosen ones. The error has `role="alert"`. | — |
| SETUP-39 | P2 | mouse + storage | The review strip's "{name} throws first" is correct for a counted entry, while the lobby's duel cards keep your card first; Dan's scheduled opponent throws first ([Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile, lobby on Cornhole. | 1. Open the dialog, set **Your demo user** to Dan, choose **Counted entry · points**.<br>2. Read the strip, close, read the duel cards.<br>3. Lock it and read the recording's participant order. | The strip reads "Doug throws first". The duel cards still show your card as **THROWS FIRST**. The recording's first participant is user Doug. | — |
| SETUP-40 | P2 | storage | A counted entry with your card set to Doug is Doug against Doug ([Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile. | 1. Open the dialog, choose **Counted entry · points** and Doug's card as yours.<br>2. Lock it and read the recording's participants. | Both participants have card Doug. On the stage both nameplates read **DOUG**. | — |
| SETUP-41 | P2 | mouse | With a tie rule that allows extra pairs, the estimate adds "+ extra pairs" ([Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile, lobby on Cornhole. | 1. Open the dialog and choose **Up to 3 extra equal pairs**. | The strip reads "Dan throws first · approximately 31 seconds + extra pairs". | — |
| SETUP-42 | P2 | mouse + storage | A ticked **Heat check** or **Replayable showcase seed** stays ticked while hidden, and applies again when the option reappears ([Edge cases](../watch/setup-dialog.md#edge-cases)). | Fresh profile, lobby on Cornhole. | 1. Tick both options.<br>2. Switch to **Counted entry · points**, then back to **Exhibition · no points**.<br>3. Close, pick **02 Football**, reopen, then close, pick **01 Cornhole**, reopen.<br>4. Lock the exhibition and read `setup`. | Both options are ticked each time they reappear. The recording has `secret` and `showcase` true and seed `velvet-paw-29`. | — |

Not checkable by hand:

- Whether the lobby's duel cards should show a counted entry's real opponent card and order is a product call ([Open questions and verification](../watch/setup-dialog.md#open-questions-and-verification)).
- A refusal while the dialog is closing, which the document says is shown only in the error box, needs the Escape of SETUP-29 and a refusal to coincide ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)).
- Locking refused with "Scoring policy changed. Review the setup again.", or loading another tab's recording for the same counted pairing instead of creating one, needs another tab's write to land after this tab last read the save but before **Start showdown**. The timing cannot be produced on demand ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt); [Open questions and verification](../watch/setup-dialog.md#open-questions-and-verification)).
- A reload during **Locking the contest…** keeps the contest only if the write had finished. The moment cannot be chosen ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)).
- `configure_arena_event` changing the sport under an open dialog, and a locked contest ignoring a later change, need a browser that ships WebMCP ([Cancel and interrupt](../watch/setup-dialog.md#cancel-and-interrupt)).

## watch/playback-controls.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PLAYBACK-01 | P2 | mouse | While a recording is loaded, the title reads **{EVENT} / LIVE**, the event dock and resume banner are hidden, the side station is headed **THE MATCH CARD**, and the stage draws a nameplate for each card ([Summary](../watch/playback-controls.md#summary)). | Fresh profile, default choices. Lock an exhibition and click the logo during the entrances, so the resume banner shows. | 1. Press **Resume contest**.<br>2. Read the page above, beside and on the stage. | The title reads **CORNHOLE / LIVE**. No event dock and no banner. The side station reads **THE MATCH CARD**. A nameplate is drawn in each top corner of the stage. | pass (scripted pass 2026-09-24, title only): the title read "CORNHOLE / LIVE". |
| PLAYBACK-02 | P1 | mouse | After **Start showdown** the stage rebuilds (**UNFOLDING THE ARENA…**), then the entrances begin: the bottom bar reads **CARDS TO COURT** and the narration "The cards are opening. Make some room." ([The simple case](../watch/playback-controls.md#the-simple-case)). | Fresh profile, default choices. | 1. Press **Start showdown**.<br>2. Watch the stage and read the bottom bar and the narration. | **UNFOLDING THE ARENA…**, then the cards walk on. The bottom bar reads **CARDS TO COURT** and **● LIVE**, and the narration is as quoted. | pass (scripted pass 2026-09-24): "CARDS TO COURT / ● LIVE" and "The cards are opening. Make some room.". |
| PLAYBACK-03 | P1 | mouse | After 2.65 seconds the attempts begin, alternating between the two cards ([The simple case](../watch/playback-controls.md#the-simple-case)). | Fresh profile, default choices, screen recording running. | 1. Lock an exhibition.<br>2. From the recording, time the first movement of the entrances to **ROUND 1**, and note who throws each attempt. | **ROUND 1** appears about 2.65 s into playback at 1×. The **THROWS FIRST** card throws first, then the cards alternate. | blocked (scripted pass 2026-09-24): game/playback time ran slow under software WebGL. |
| PLAYBACK-04 | P2 | mouse | The side station has four buttons: pause, a speed button reading **1×**, skip to result and attempt history; during the entrances a **Skip entrances** link appears under them ([The simple case](../watch/playback-controls.md#the-simple-case)). | Fresh profile, default choices. | 1. Lock an exhibition.<br>2. During the entrances, read the controls under the narration. | Four buttons, named "Pause playback", "Playback speed" (reading **1×**), "Skip to result" and "Attempt history", with **Skip entrances** under them. | pass (scripted pass 2026-09-24): **Skip entrances** present and the first button read "Pause playback". |
| PLAYBACK-05 | P1 | mouse | The speed resets to **1×** whenever a recording is loaded, replayed or resumed ([Starting](../watch/playback-controls.md#starting)). | Fresh profile, default choices. | 1. Lock an exhibition, set **2×**, press **Skip to result**, then **Replay same recording**.<br>2. Set **2×**, click the logo, press **Resume contest**. | The speed button reads **1×** after the replay and after the resume. | pass (scripted pass 2026-09-24, resume only): **Resume contest** opened at 1×. |
| PLAYBACK-06 | P1 | mouse | After **Start showdown** the contest plays once the stage is ready *and* the setup dialog has finished closing ([Starting](../watch/playback-controls.md#starting)). | Fresh profile, default choices. | 1. Press **Start showdown**.<br>2. Watch the dialog's closing, the stage and the pause button. | The clock does not move while either is unfinished: the progress bar stays empty. Then the entrances play by themselves and the button reads "Pause playback". | pass (scripted pass 2026-09-24): under software rendering the dialog took about 48 s to close, and playback waited for it. |
| PLAYBACK-07 | P2 | mouse | **Replay** from History plays once the stage is ready; **Replay same recording** plays at once from second 0 without rebuilding the stage ([Starting](../watch/playback-controls.md#starting)). | Fresh profile. | 1. History, **Replay** on the basketball row; do not touch the controls.<br>2. **Skip to result**, then **Replay same recording**. | Step 1: **UNFOLDING THE ARENA…**, then the entrances start by themselves. Step 2: the entrances restart at once, with no **UNFOLDING THE ARENA…**. | — |
| PLAYBACK-08 | P2 | mouse + storage | The logo, before anything plays, writes the position at second 0, so the lobby offers the contest on the resume banner ([Backing out at once](../watch/playback-controls.md#backing-out-at-once)). | Fresh profile, default choices. | 1. Press **Start showdown**.<br>2. While **UNFOLDING THE ARENA…** still shows, click the logo.<br>3. Read the lobby and `active`. | The banner reads "Saved at 0 seconds. Your result is waiting." `active` names the new recording with time 0. | — |
| PLAYBACK-09 | P1 | mouse + storage | Loading another recording makes *that* recording the one waiting to resume; this contest's position is dropped, and its points are revealed without it being watched ([Backing out at once](../watch/playback-controls.md#backing-out-at-once)). | Fresh profile, default choices. | 1. Lock a counted entry and let it play about 5 s.<br>2. History, **Replay** on the basketball row, and let it play a few seconds.<br>3. Read `active`.<br>4. **Skip to result**, **Next showdown**, and read the chip and the lobby. | `active` names the basketball recording, not the counted entry. In the lobby, no banner, and the chip's points include Doug's award for the counted entry: 3 plus that award's points in `ledger`. (B-05) | — |
| PLAYBACK-10 | P1 | mouse | Switching tab pauses playback, keeps the recording and stops sound; returning shows the contest paused where it was, after the stage rebuilds ([Backing out at once](../watch/playback-controls.md#backing-out-at-once); [Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition and turn Watch sound on. | 1. At about ROUND 2, switch to **Standings**.<br>2. Wait 10 s and switch back to **Watch**. | Sound stops at the switch. On return, **UNFOLDING THE ARENA…**, then the same moment, frozen. The bottom bar reads **PAUSED** and the button "Resume playback". | pass (scripted pass 2026-09-24): back on Watch, the button read "Resume playback" and the bottom bar **PAUSED**. Sound not checked. |
| PLAYBACK-11 | P1 | storage | The position is written to this browser's save every 2 seconds of playback, so about once a real second at **2×** ([Committing](../watch/playback-controls.md#committing); [Edge cases](../watch/playback-controls.md#edge-cases)). | Fresh profile, default choices, developer tools open on localStorage. | 1. Lock an exhibition.<br>2. Refresh the view of `active.time` every second for 10 s.<br>3. Set **2×** and repeat. | At 1× `active.time` advances in steps of about 2, about every 2 s. At 2× it advances in steps of about 2, about every 1 s. | blocked (scripted pass 2026-09-24): game/playback time ran slow under software WebGL. |
| PLAYBACK-12 | P1 | mouse | Counted-entry points stay hidden until playback is complete ([Committing](../watch/playback-controls.md#committing)). | Fresh profile, default choices. | 1. Lock a counted entry.<br>2. During the attempts, switch to **Standings**. | Doug has 3 points and **Played** 1; Dan has 0 and **Played** 1: the new contest is not counted. | pass (scripted pass 2026-09-24): Standings during playback showed Doug 3, played 1. |
| PLAYBACK-13 | P1 | mouse | **Pause playback** stops the stage on its current frame, stops sound and shows **PAUSED** on the right of the bottom bar; **Resume playback** continues from the same moment ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices, Watch sound on. Lock an exhibition. | 1. During a bag's flight, press "Pause playback" and wait 5 s.<br>2. Press "Resume playback". | The bag hangs in the air, the progress bar stops, sound stops, and the bottom bar reads **PAUSED**. On resume the bag continues from the same point and **● LIVE** returns. | — |
| PLAYBACK-14 | P1 | mouse | The speed button cycles **1×** → **2×** → **0.5×** → **1×** ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. Press the speed button three times, reading it after each press. | **2×**, **0.5×**, **1×**. | pass (scripted pass 2026-09-24): the speed cycled 1× → 2× → 0.5× → 1×. |
| PLAYBACK-15 | P3 | mouse | The speed changes how fast the clock runs, including the entrances and the finale ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices, screen recording running. | 1. Set **2×** at once after **Start showdown** and time the entrances.<br>2. Replay, set **0.5×**, and time them again. | About 1.3 s at 2× and about 5.3 s at 0.5×, against 2.65 s at 1×. | blocked (scripted pass 2026-09-24): game/playback time ran slow under software WebGL. |
| PLAYBACK-16 | P1 | mouse | **Skip entrances**, shown only during the first 2.65 seconds, jumps to the start of the first attempt ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. During the entrances, press **Skip entrances**. | The bottom bar reads **ROUND 1**, the first card is ready to throw, and **Skip entrances** is gone. | pass (scripted pass 2026-09-24): **Skip entrances** took the bottom bar to "ROUND 1". |
| PLAYBACK-17 | P1 | mouse + storage | **Skip to result** jumps to the very end, past the finale: playback is complete at once, the points are revealed, the position is cleared and any sound stops ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock a counted entry with Watch sound on. | 1. During ROUND 1, double-click "Skip to result".<br>2. Read the chip, the result panel and `active`. | The result panel appears at once, the progress bar is full, and there is no finale. The chip includes Doug's new award. `active` is null, and sound stops. The ledger still has 6 awards. | pass (scripted pass 2026-09-24): the chip read "6 CLUB POINTS", the result panel appeared and the waiting contest became null. Sound and the double-click were not checked. |
| PLAYBACK-18 | P2 | mouse | **The contest, as it happened** lists every attempt that has landed so far, numbered, with the card, what it hit, the commentary and **+N**; before the first landing it reads "The first attempt has not landed yet." ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. During the entrances, press "Attempt history"; close it.<br>2. After ROUND 2, open it again. | Step 1: "The first attempt has not landed yet.". Step 2: numbered entries for the attempts that have landed only, each with the card, a word such as "hole" or "board", the commentary and **+N**. | pass (scripted pass 2026-09-24, step 1 only): "THE CONTEST, AS IT HAPPENED … The first attempt has not landed yet.". |
| PLAYBACK-19 | P2 | mouse | **Export immutable recording** downloads the whole recording as JSON, including attempts not yet shown ([While playing](../watch/playback-controls.md#while-playing)). | As PLAYBACK-18, during ROUND 1. | 1. Open "Attempt history" and press **Export immutable recording**.<br>2. Open the file. | A JSON file with every attempt of the contest, 8 for a Cornhole contest without extra pairs, its scores and its winner. Playback keeps running behind the dialog. | — |
| PLAYBACK-20 | P1 | mouse | Watch sound starts off; the speaker button at the stage's bottom right, labelled "Enable sound" or "Mute sound", turns it on or off, and the footer's **Sound on/off** mirrors it ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices, speakers on. | 1. Lock an exhibition and listen to the entrances.<br>2. Press the speaker button and read the footer.<br>3. Press it again. | No sound until step 2. The button is named "Enable sound", then "Mute sound", then "Enable sound" again, and the footer changes with it. Step 2 plays the cues. | — |
| PLAYBACK-21 | P2 | mouse | The clean spectator view hides the header and tabs, the title, the event dock, the side station, the floor caption and the footer; the progress bar stays, and the same button brings everything back. While it is on, the viewer cannot pause, change speed or skip ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. Press the clean-view button beside the speaker.<br>2. Look for every hidden part and any playback control.<br>3. Press it again. | The stage fills the page's width with the progress bar under it. None of the listed parts and no pause, speed or skip control can be reached. The second press restores everything. | pass (scripted pass 2026-09-24): the header, the playback controls and the footer were hidden; the progress bar and the clean-view button stayed. |
| PLAYBACK-22 | P2 | mouse | During the entrances the nameplates show each card's first name, revealed score, pips for attempts left and a status such as **4 BAGS LEFT · WAITING**, and the sign shows the event's name over **CARDS TO COURT** ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. | 1. Lock an exhibition.<br>2. During the entrances, read both nameplates and the sign. | Nameplates: **DAN** and **DOUG**, each 0, four pips, **4 BAGS LEFT · WAITING**. Sign: **CORNHOLE** over **CARDS TO COURT**. | pass (scripted pass 2026-09-24): "DAN 0 · 4 BAGS LEFT · WAITING" and "CORNHOLE / CARDS TO COURT". |
| PLAYBACK-23 | P1 | mouse | The bottom bar reads **CARDS TO COURT** during the entrances and **ROUND n** during the attempts, with **● LIVE** or **PAUSED** on the right ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. Read the bar during the entrances.<br>2. Read it during ROUND 1 and ROUND 2, once pausing. | **CARDS TO COURT** / **● LIVE**, then **ROUND 1**, **ROUND 2** / **● LIVE**, and **PAUSED** while paused. | pass (scripted pass 2026-09-24): "CARDS TO COURT / ● LIVE", then "ROUND 1" after **Skip entrances**. |
| PLAYBACK-24 | P1 | storage | The page's own "Live score" scoreboard cannot be seen at any width; screen readers still read it ([While playing](../watch/playback-controls.md#while-playing)). | Fresh profile, default choices. Lock an exhibition. | 1. Look over the stage for a score box reading "n/4 bags".<br>2. In developer tools, find the element named "Live score" and read its rendered size.<br>3. Repeat at 390 px wide. | Nothing but the drawn nameplates shows the score. The "Live score" element is a one-pixel box at both widths and is in the accessibility tree. | pass (scripted pass 2026-09-24): covered by the stage's drawing; viewers saw the drawn nameplates and sign (document corrected after this pass). |
| PLAYBACK-25 | P2 | mouse | Only the side station's note and the floor caption's code word tell a sighted viewer whether the contest counts: "Points post once. Replay as often as you like." or "EXHIBITION / NO LEADERBOARD POINTS", and "COLLECTIONS: {user} / {user} / {mode}" with `ranked` or `exhibition` ([While playing](../watch/playback-controls.md#while-playing); [Edge cases](../watch/playback-controls.md#edge-cases)). | Fresh profile, default choices. | 1. Lock an exhibition; read the note and the floor caption.<br>2. **Skip to result**, **Next showdown**, lock a counted entry, and read them again. | Exhibition: "EXHIBITION / NO LEADERBOARD POINTS" and "COLLECTIONS: Doug / Dan / exhibition". Counted: "Points post once. Replay as often as you like." and "COLLECTIONS: Doug / Dan / ranked". Nothing drawn on the stage says which. (B-36) | pass (scripted pass 2026-09-24, exhibition only): "EXHIBITION / NO LEADERBOARD POINTS" and "COLLECTIONS: Doug / Dan / exhibition". |
| PLAYBACK-26 | P2 | mouse | At completion the title becomes **{EVENT} / FINAL**, all playback buttons except **Attempt history** disappear, and so does the side station's note ([Resolving](../watch/playback-controls.md#resolving)). | Fresh profile, default choices. Lock an exhibition. | 1. **Skip to result**.<br>2. Read the title and the side station. | **CORNHOLE / FINAL**. Only "Attempt history" remains, and no note. | pass (scripted pass 2026-09-24): "CORNHOLE / FINAL", and only attempt history stayed visible. |
| PLAYBACK-27 | P2 | mouse | At completion the narration announces the winner, "{card's full name} takes it.", or "Honors shared. The rivalry continues." ([Resolving](../watch/playback-controls.md#resolving)). | Fresh profile, default choices. Lock an exhibition. | 1. Watch to the end, or **Skip to result**.<br>2. Read the narration and compare it with `winner`. | The winning card's full name, for example "Doug Weidensaul takes it.", or the draw line when `winner` is null. | — |
| PLAYBACK-28 | P1 | mouse | The finale keeps animating for about 2.6 seconds after completion, unless the viewer skipped to the result, which skips the victory animation ([Resolving](../watch/playback-controls.md#resolving); [Edge cases](../watch/playback-controls.md#edge-cases)). | Fresh profile, default choices, screen recording running. Lock exhibitions until one is decided. | 1. Watch it to the end.<br>2. Time from the result panel appearing to the stage coming to rest.<br>3. **Replay same recording**, then **Skip to result**. | About 2.6 s of winner celebration after the panel appears, with the progress bar still filling. After **Skip to result** the stage is already at rest. | blocked (scripted pass 2026-09-24): game/playback time ran slow under software WebGL. |
| PLAYBACK-29 | P2 | keyboard | There are no keyboard shortcuts: Space does not pause and the arrow keys do not seek ([Modifiers](../watch/playback-controls.md#modifiers)). | Fresh profile, default choices. Lock an exhibition, then click the page background. | 1. Press Space, then Left and Right. | Playback carries on unchanged and the progress bar does not jump. | — |
| PLAYBACK-30 | P2 | mouse | Toggling Reduced motion during playback applies at once: no contact effects or camera punch and shake, and calmer clips ([Modifiers](../watch/playback-controls.md#modifiers)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, open Arena settings, tick **Reduced motion**, close.<br>2. Watch a contact. | No rebuild. From the next contact there is no ring burst and no camera punch. Playback did not pause. | — |
| PLAYBACK-31 | P2 | mouse | Toggling Lower graphics quality during playback rebuilds the stage, and playback carries on from the same moment once it is ready ([Modifiers](../watch/playback-controls.md#modifiers)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 2, tick **Lower graphics quality** and close the dialog.<br>2. Watch the stage and the progress bar. | **UNFOLDING THE ARENA…**, then the same moment of ROUND 2, carrying on. The progress bar does not go back. | — |
| PLAYBACK-32 | P2 | small screen | Resizing rescales the stage, keeping 16:9, without interrupting playback ([Modifiers](../watch/playback-controls.md#modifiers)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, change the width from 1440 px to 600 px and back. | The stage keeps its 16:9 shape at each width. There is no rebuild and no pause, and the progress bar keeps filling. | — |
| PLAYBACK-33 | P2 | two tabs | Another tab's writes, including its reset, leave this tab's playback running ([Modifiers](../watch/playback-controls.md#modifiers); [Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, tabs A and B. In A, lock an exhibition. | 1. In B, lock another exhibition.<br>2. In B, **Reset demo data**.<br>3. Watch A throughout. | A keeps playing through both with no pause and no rebuild. After the reset, A's recording is no longer in `recordings`. | — |
| PLAYBACK-34 | P2 | keyboard | Playback keeps running behind a dialog, and Escape closes it without affecting playback ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition. | 1. Open "Attempt history", then House rules, then Arena settings, then History, closing each with Escape. | Behind each dialog, the attempts carry on and the progress bar fills. Escape closes each dialog, and playback is not paused. | — |
| PLAYBACK-35 | P2 | mouse | While the stage is still loading, the pause button offers **Resume playback**, and pressing it starts the clock at once, before the stage is drawn ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt); [Open questions and verification](../watch/playback-controls.md#open-questions-and-verification)). | Fresh profile, default choices; CPU throttling in developer tools so that loading is slow. | 1. Press **Start showdown**.<br>2. While **UNFOLDING THE ARENA…** shows, press "Resume playback". | The progress bar starts filling before the stage appears, and the stage then shows a moment after the start of the entrances. | — |
| PLAYBACK-36 | P2 | mouse | **Skip to result** works even before playback starts ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | As PLAYBACK-35. | 1. While **UNFOLDING THE ARENA…** shows, press "Skip to result". | The result panel appears with the recording's final score, and `active` is null. | — |
| PLAYBACK-37 | P1 | hidden tab | Hiding the browser tab stops the clock without saying **PAUSED** and without stopping sound; playback carries on by itself, without jumping, when the tab is visible again ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices, Watch sound on. Lock an exhibition. | 1. During ROUND 1, note the progress; switch to another browser tab for 20 s.<br>2. Switch back at once and watch. | The progress is where it was, and the bottom bar still reads **● LIVE**. It advances again with no press and no jump. A tone already playing is not cut off, and no new cue fires while hidden. | pass (scripted pass 2026-09-24, emulated `document.hidden`): the progress stayed at 2% with **● LIVE**, then advanced to 4% once visible. Sound not checked. |
| PLAYBACK-38 | P1 | blur | Losing window focus without hiding the tab has no effect ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition; another application's window beside the browser. | 1. During ROUND 1, click the other window and watch the Arena for 10 s. | Playback carries on, and the bottom bar reads **● LIVE**. | — |
| PLAYBACK-39 | P2 | mouse | **Reset demo** in this tab stops playback, unloads the recording and returns to the empty lobby ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, Arena settings, **Reset demo…**, **Reset demo data**.<br>2. Close the dialogs. | The lobby, with no banner and 2 recordings in the save. | — |
| PLAYBACK-40 | P2 | storage | A lost graphics context pauses playback and shows **Graphics paused. Restore the arena to resume the saved contest.**; **Restore arena** reloads the same second, paused ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 2, lose the graphics context from the console.<br>2. Read the error box; press **Restore arena**. | The clock stops and the error box shows the message. After **Restore arena**: **UNFOLDING THE ARENA…**, then the same moment, paused, with "Resume playback". | — |
| PLAYBACK-41 | P1 | storage | After a lost graphics context the clock is paused, but the pause button still says **Pause playback** until **Restore arena** is pressed ([Open questions and verification](../watch/playback-controls.md#open-questions-and-verification)). | As PLAYBACK-40, after step 1. | 1. Read the pause button's name.<br>2. Press it once and watch the progress bar. | Record what happens (suspected bug). The document says the button still reads "Pause playback", and the first press *resumes* the stopped clock. (B-14) | — |
| PLAYBACK-42 | P2 | storage | If writing the position fails, the error box reads **Playback could not be saved. Keep this tab open and export your recording.**, and playback continues ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition. | 1. During the entrances, fill localStorage to its quota.<br>2. Wait 4 s. | The error box shows the message, and playback keeps running. | — |
| PLAYBACK-43 | P2 | storage | The narration is a polite live region, the progress bar a `progressbar` labelled "Contest playback" with a percentage, and the buttons are labelled "Pause playback" or "Resume playback", "Playback speed", "Skip to result" and "Attempt history" ([Interactions with other systems](../watch/playback-controls.md#interactions-with-other-systems)). | Fresh profile, default choices. Lock an exhibition. | 1. Read the accessibility tree during ROUND 1. | The roles, names and live-region politeness as quoted, with the progress bar's value changing. | — |
| PLAYBACK-44 | P2 | two tabs | Two tabs playing the same recording both write positions, and the last write wins ([Interactions with other systems](../watch/playback-controls.md#interactions-with-other-systems)). | Fresh profile, tabs A and B. | 1. In A, History, **Replay** on the basketball row.<br>2. About 10 s later, do the same in B.<br>3. Pause A and let B play; read `active` twice, 4 s apart. | `active` names the basketball recording, and its time follows B's playback, not A's paused second. | — |
| PLAYBACK-45 | P1 | mouse | There is no seek: the progress bar cannot be dragged, and nothing rewinds except **Replay same recording** after completion ([Edge cases](../watch/playback-controls.md#edge-cases)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, click the progress bar near its end, then drag along it. | Nothing jumps; playback carries on from where it was. | — |
| PLAYBACK-46 | P2 | mouse | Pausing during the finale pauses the finale animation; the result panel is already showing ([Edge cases](../watch/playback-controls.md#edge-cases)). | Fresh profile, default choices. Lock exhibitions until one is decided. | 1. Watch it to the end.<br>2. As the result panel appears, look for a pause control in the side station.<br>3. Within 1 s, switch to **Standings** and back. | Record what happens. The document's own [Resolving](../watch/playback-controls.md#resolving) says only "Attempt history" remains at completion, so a tab switch may be the only way to pause. After it, the result panel shows and the celebration is frozen. | — |

Not checkable by hand:

- Whether the clean spectator view should leave no way to pause is a product call ([Open questions and verification](../watch/playback-controls.md#open-questions-and-verification); B-36).
- That each card's personality makes recordings run for different lengths is a statement about the simulation, not a single observable result ([Modifiers](../watch/playback-controls.md#modifiers)).
- A stage that fails to load for a recording cannot be produced on demand ([Cancel and interrupt](../watch/playback-controls.md#cancel-and-interrupt)).
- `read_arena` returning the revealed score, and `configure_arena_event` refusing while a recording is loaded, need a browser that ships WebMCP ([Interactions with other systems](../watch/playback-controls.md#interactions-with-other-systems)). The scripted pass of 2026-09-24 saw the refusal with an emulated `document.modelContext`.

## watch/result-and-replay.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RESULT-01 | P1 | mouse | The result panel appears below the stage the moment playback is complete, when the last attempt ends or the viewer presses **Skip to result** ([Summary](../watch/result-and-replay.md#summary)). | Fresh profile, default choices. Lock an exhibition. | 1. Watch until the last attempt's score shows.<br>2. **Replay same recording**, then **Skip to result**. | Both times the dark result panel appears under the stage at once. It is not there before completion. | pass (scripted pass 2026-09-24, **Skip to result** only): the panel appeared at once. |
| RESULT-02 | P1 | mouse | For a counted entry, completion reveals its points everywhere: the chip, Standings, member records and History ([Summary](../watch/result-and-replay.md#summary); [Starting](../watch/result-and-replay.md#starting)). | Fresh profile, default choices. Lock a counted entry. | 1. **Skip to result**.<br>2. Read the chip, Standings, Doug's member record and History. | Each now includes the contest: the chip and Standings add Doug's award from `ledger`, **Played** is 2 for Doug and Dan, and History lists a cornhole row. | pass (scripted pass 2026-09-24, chip only): the chip read "6 CLUB POINTS". |
| RESULT-03 | P2 | mouse | At completion the bottom bar reads **FINAL SCORE** and **FULL TIME** ([The simple case](../watch/result-and-replay.md#the-simple-case)). | As RESULT-01. | 1. **Skip to result**.<br>2. Read the bottom bar. | **FINAL SCORE** on the left and **FULL TIME** on the right. No **● LIVE** or **PAUSED**. | — |
| RESULT-04 | P2 | mouse + storage | A counted result panel shows **COUNTED RESULT / POINTS POSTED**, "{score} — {score} {event}", and for each user their name, **+{points} PTS** and "{total} total / Rank {n}" ([Starting](../watch/result-and-replay.md#starting)). | Fresh profile, default choices. Lock a counted entry. | 1. **Skip to result**.<br>2. Compare the panel with the recording's `scores` and the new awards in `ledger`. | The eyebrow as quoted. The score line matches `scores` and names **Cornhole**. The Doug and Dan columns show their awards' points and their revealed totals and ranks. | pass (scripted pass 2026-09-24): "COUNTED RESULT / POINTS POSTED … 3 — 2 Cornhole · Doug +3 PTS 6 total / Rank 1 · Dan +0 PTS 0 total / Rank 4". |
| RESULT-05 | P2 | mouse | An exhibition result panel shows **EXHIBITION / NO LEADERBOARD POINTS** and **+0 PTS** for each user with "{total} total / Rank {n}" ([Starting](../watch/result-and-replay.md#starting)). | Fresh profile, default choices. Lock an exhibition. | 1. **Skip to result**. | The eyebrow as quoted. Both columns read **+0 PTS**, with the unchanged totals (Doug 3 total / Rank 1, Dan 0 total / Rank 4) and no "(was". | pass (scripted pass 2026-09-24): "+0 PTS" for both users. |
| RESULT-06 | P1 | mouse + storage | The headline names the winning *card*, "{card's first name} WINS.", while the columns name the demo *users* ([Starting](../watch/result-and-replay.md#starting)). | Fresh profile, default choices: user Doug with Dan's card against user Dan with Doug's card. Lock a counted entry. | 1. **Skip to result**.<br>2. Compare the headline with the column showing the win points and with `winner`. | Record what happens (suspected bug). The document says the headline gives the winning card's first name, so when user Doug's Dan card wins it reads **Dan WINS.** above "Doug **+3 PTS**". (B-02) | confirmed (scripted pass 2026-09-24): **Dan WINS.** with Doug +3 PTS and Dan +0 PTS. For an exhibition, **Doug WINS.** when user Dan's side won. |
| RESULT-07 | P1 | mouse | When both users play the same card, for example counted Doug against Doug, the headline does not say which user won ([Starting](../watch/result-and-replay.md#starting); [Open questions and verification](../watch/result-and-replay.md#open-questions-and-verification)). | Fresh profile. Open the dialog, choose **Counted entry · points** and Doug's card as yours, and lock. | 1. **Skip to result**.<br>2. Read the headline and the columns. | Record what happens (suspected bug). The document says the headline reads **Doug WINS.** whichever user won, and only the columns tell. (B-02) | — |
| RESULT-08 | P1 | storage | The position is cleared at completion, with the next position write, so the contest is no longer waiting to resume ([Starting](../watch/result-and-replay.md#starting)). | Fresh profile, default choices, developer tools on localStorage. Lock an exhibition. | 1. Watch it to the end without skipping.<br>2. Read `active` within 3 s of the result panel appearing.<br>3. Click the logo. | `active` is null, and the lobby shows no resume banner. | — |
| RESULT-09 | P2 | mouse | Leaving right away, by a tab switch, the logo or a reload, keeps everything; the points stay revealed and there is nothing to resume ([Backing out at once](../watch/result-and-replay.md#backing-out-at-once); [Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock a counted entry and **Skip to result**. | 1. Switch to **Standings** and back.<br>2. Click the logo.<br>3. Reload. | After each, the chip still includes the new points. No resume banner after the logo or the reload. | — |
| RESULT-10 | P2 | mouse | **Replay same recording** starts the same recording again from second 0 at **1×**, playing at once without rebuilding the stage ([Committing](../watch/result-and-replay.md#committing)). | Fresh profile, default choices. Lock an exhibition, set **2×**, **Skip to result**. | 1. Press **Replay same recording**. | The entrances restart at once with no **UNFOLDING THE ARENA…**. The speed reads **1×**, and the progress bar starts from empty. | — |
| RESULT-11 | P1 | mouse | From a replay's start, the page treats the contest as unfinished again: the result panel disappears and the contest's points are hidden until the replay completes ([Committing](../watch/result-and-replay.md#committing); [Edge cases](../watch/result-and-replay.md#edge-cases)). | Fresh profile, default choices. Lock a counted entry, **Skip to result**, and note the chip. | 1. Press **Replay same recording**.<br>2. Read the chip and Standings during the replay. | Record what happens (suspected bug). The document says the panel goes, the chip drops back to its total before this contest, and Standings lose the points, although the result was already revealed. (B-01) | confirmed (scripted pass 2026-09-24): the chip went from 6 to 3 and the result panel was removed. |
| RESULT-12 | P2 | mouse | A replay shows exactly the same attempts, because it is the same recording ([While committed](../watch/result-and-replay.md#while-committed)). | Fresh profile, default choices. Lock an exhibition and watch it to the end with "Attempt history" open at the end. | 1. Note every entry in **The contest, as it happened**.<br>2. **Replay same recording**, watch to the end, and compare. | The same attempts in the same order, with the same hits, commentary, **+N** and final score. | — |
| RESULT-13 | P2 | mouse | **Next showdown** stops sound, clears the waiting contest, unloads the recording and shows the lobby, keeping the setup choices ([Resolving](../watch/result-and-replay.md#resolving)). | Fresh profile. Choose **Bold · wider swings** in the dialog, lock an exhibition, turn Watch sound on, and watch to the finale. | 1. During the finale, press **Next showdown**.<br>2. Read `active`, then open **Set up showdown**. | Sound stops at once. The lobby shows no banner and `active` is null. The dialog still has **Bold · wider swings**. | — |
| RESULT-14 | P2 | mouse | A replay that reaches its end comes back to the same result panel ([Resolving](../watch/result-and-replay.md#resolving)). | As RESULT-12, with the panel's text noted. | 1. **Replay same recording** and watch it to the end. | The same result panel, with the same headline, score and columns. | — |
| RESULT-15 | P1 | storage | A replay of either kind awards nothing new ([Modifiers](../watch/result-and-replay.md#modifiers)). | Fresh profile, default choices. Lock a counted entry and **Skip to result**. Note the length of `ledger` (6). | 1. **Replay same recording** and let it complete.<br>2. Replay the basketball row from History and **Skip to result**.<br>3. Read `ledger`. | Still 6 awards, none added or changed. The chip shows the same total as after the first completion. | — |
| RESULT-16 | P2 | mouse | In the clean spectator view the result panel stays visible under the full-window stage ([Modifiers](../watch/result-and-replay.md#modifiers)). | Fresh profile, default choices. Lock an exhibition. | 1. Turn on the clean spectator view.<br>2. Wait for the end. | The result panel appears below the stage, with **Next showdown** and **Replay same recording** usable. Everything else stays hidden. | — |
| RESULT-17 | P3 | small screen | At 1150 px and below, the result panel's buttons move under the scores ([Modifiers](../watch/result-and-replay.md#modifiers)). | As RESULT-05, with the panel showing. | 1. Set the width to 1151 px, then 1150 px. | At 1151 px the buttons sit to the right of the scores. At 1150 px they are below them. | — |
| RESULT-18 | P2 | two tabs | Another tab's write updates the result panel's totals and ranks in place ([Modifiers](../watch/result-and-replay.md#modifiers)). | Fresh profile, tabs A and B. In A, lock counted Cornhole and **Skip to result**. | 1. In B, choose **02 Football**, lock the counted entry and **Skip to result**.<br>2. Look at A's result panel without touching it. | A's "{total} total / Rank {n}" for Doug and Dan now include the football awards. Their **+N PTS** stay as they were. | — |
| RESULT-19 | P2 | keyboard | During a replay a dialog on top leaves it playing, and Escape closes the dialog ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock an exhibition, **Skip to result**, **Replay same recording**. | 1. Open "Attempt history" and watch behind it.<br>2. Press Escape. | The replay keeps playing behind the dialog, and Escape closes it without pausing. | — |
| RESULT-20 | P2 | mouse | During a replay the pause button is back and works normally ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-19. | 1. Press "Pause playback", then "Resume playback". | All four playback buttons are back. Pausing shows **PAUSED**, and resuming continues. | — |
| RESULT-21 | P2 | mouse | **Replay same recording** pressed repeatedly restarts from second 0, and **Next showdown** can be pressed only once because the panel goes away ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-10. | 1. Double-click **Replay same recording**.<br>2. **Skip to result**, then double-click **Next showdown**. | One replay from second 0. The lobby shows once; the second click reaches nothing and no dialog opens. | — |
| RESULT-22 | P2 | mouse | Switching tab and back shows the result panel again after the stage rebuilds, and the logo returns to the lobby like **Next showdown** ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-05, with the panel showing. | 1. Switch to **Standings** and back.<br>2. Click the logo. | Step 1: **UNFOLDING THE ARENA…**, then the same result panel. Step 2: the lobby with no banner. | — |
| RESULT-23 | P1 | mouse | Leaving a replay by the logo leaves it *waiting to resume*, with its points hidden again ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt); [Edge cases](../watch/result-and-replay.md#edge-cases)). | As RESULT-11, after step 1. | 1. During the replay's entrances, click the logo.<br>2. Read the lobby and the chip. | Record what happens (suspected bug). The document says the lobby offers "Saved at N seconds. Your result is waiting." with **Resume contest** for a result already seen, and the chip leaves out that contest's points. (B-01) | confirmed (scripted pass 2026-09-24): the banner read "Saved at 0 seconds. Your result is waiting." with the chip still at 3. |
| RESULT-24 | P2 | mouse | Mid-replay, loading another recording takes over the waiting slot, so the replayed contest's points are shown again ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-11, after step 1. | 1. Open History and **Replay** the basketball row.<br>2. **Skip to result**, **Next showdown**, and read the chip. | The chip includes the counted cornhole points again, and there is no banner. | — |
| RESULT-25 | P2 | mouse | **Skip to result** ends a replay at once and brings the result panel back ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-11, after step 1. | 1. Press "Skip to result". | The same result panel, and the chip includes the points again. | — |
| RESULT-26 | P1 | mouse | Reloading mid-replay writes the position, and the lobby then shows "Your result is waiting." for a contest that already finished ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | As RESULT-11, after step 1. | 1. During ROUND 1 of the replay, reload. | Record what happens (suspected bug). The document says the banner "Saved at N seconds. Your result is waiting." appears, and the chip leaves out the contest's points. (B-01) | — |
| RESULT-27 | P2 | mouse | **Reset demo** unloads the recording and removes the contest and its points ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock a counted entry and **Skip to result**. | 1. Arena settings, **Reset demo…**, **Reset demo data**.<br>2. Close the dialogs. | The result panel is gone, and the lobby shows "3 CLUB POINTS" and "3 entries left". The save has 2 recordings and 4 awards. | pass (scripted pass 2026-09-24): recordings went from 3 to 2 with 4 awards, the result panel was removed, and the chip read "3 CLUB POINTS · 3 entries left". |
| RESULT-28 | P2 | storage | A lost graphics context during the finale pauses it and shows the error box, and the result panel stays ([Cancel and interrupt](../watch/result-and-replay.md#cancel-and-interrupt)). | Fresh profile, default choices. Lock exhibitions until one is decided, and watch it to the end. | 1. As the result panel appears, lose the graphics context from the console. | The celebration stops and the error box shows **Graphics paused. Restore the arena to resume the saved contest.**, with the result panel still showing. | — |
| RESULT-29 | P2 | mouse | **+N PTS** comes from the policy saved with the contest, so a later policy change never alters it, while totals and ranks use today's ledger ([Interactions with other systems](../watch/result-and-replay.md#interactions-with-other-systems); [Edge cases](../watch/result-and-replay.md#edge-cases)). | Fresh profile, default choices. Lock a counted entry. | 1. During playback, Arena settings: **Win** 5, **Draw** 2, **Save for future entries**.<br>2. **Skip to result**. | The columns show the values of the policy the contest was locked under: +3 for a win, +1 for a draw, +0 for a loss. | — |
| RESULT-30 | P2 | mouse | With Watch sound on, a decided contest's finale plays a victory fanfare, a draw has none, and **Next showdown** stops any sound ([Interactions with other systems](../watch/result-and-replay.md#interactions-with-other-systems)). | Fresh profile, speakers on, Watch sound on. Lock exhibitions until one is decided and one is a draw (**HONORS SHARED.**). | 1. Watch each to the end.<br>2. During a fanfare, press **Next showdown**. | A fanfare after the decided contest's last attempt and none after the draw. **Next showdown** cuts the fanfare off. | — |
| RESULT-31 | P2 | storage | The headline is a heading, the narration announces the winner, and the result panel is not announced as a live region by itself ([Interactions with other systems](../watch/result-and-replay.md#interactions-with-other-systems)). | Fresh profile, default choices. Lock an exhibition. | 1. **Skip to result**.<br>2. Read the accessibility tree. | The headline has a heading role, and the panel has no live-region attribute. The narration's live region holds the winner line. | — |
| RESULT-32 | P1 | two tabs | Another tab with nothing loaded hides the points while the contest is the save's waiting one, and reveals them when this tab's completion clears the slot ([Interactions with other systems](../watch/result-and-replay.md#interactions-with-other-systems)). | Fresh profile, tabs A and B on the lobby. | 1. In A, lock a counted entry.<br>2. Read B's chip.<br>3. In A, **Skip to result**; read B's chip again. | Step 2: B reads "3 CLUB POINTS" and "2 entries left". Step 3: within a second or two, B's chip includes Doug's new award. | pass (scripted pass 2026-09-24, step 2 only): B's chip went to "2 entries left" with the points still hidden. |
| RESULT-33 | P2 | mouse | A draw shows **HONORS SHARED.** ([Edge cases](../watch/result-and-replay.md#edge-cases)). | Fresh profile, default choices. | 1. Lock exhibitions with **Finish as a draw**, pressing **Skip to result** and then **Next showdown** each time, until a result panel shows a level score (`winner` null). | That panel's headline reads **HONORS SHARED.** and the narration "Honors shared. The rivalry continues.". No card's name is given as the winner. | — |
| RESULT-34 | P2 | mouse | "(was {n})" appears only for counted entries, and only when the user's rank actually changed ([Edge cases](../watch/result-and-replay.md#edge-cases)). | Fresh profile. Open the dialog, set **Your demo user** to Sam, choose **Counted entry · points**: Sam against Riley, both 1 point, rank 2. | 1. Lock it and **Skip to result**.<br>2. Read both columns. | If the contest is decided: the winner "4 total / Rank 1 (was 2)" and the loser "1 total / Rank 3 (was 2)". If it is drawn: both "2 total / Rank 2" with no "(was". Exhibitions (RESULT-05) never show it. | — |

Not checkable by hand:

- Whether the headline should name the user, the card, or both, is a product call ([Open questions and verification](../watch/result-and-replay.md#open-questions-and-verification); B-02).
- An *unresolved draw*, " / Unresolved draw after three extra pairs", needs a contest still tied after three extra pairs. It cannot be produced on demand ([Starting](../watch/result-and-replay.md#starting); [Edge cases](../watch/result-and-replay.md#edge-cases)).
- A counted draw giving each user the draw points, 1 by default, needs a scheduled pairing that happens to end level. The eight pairings' outcomes are fixed by the schedule's seeds ([Edge cases](../watch/result-and-replay.md#edge-cases)).
- `read_arena` reporting `complete: true` and the final revealed score needs a browser that ships WebMCP ([Interactions with other systems](../watch/result-and-replay.md#interactions-with-other-systems)).

## watch/resume-a-contest.md

| ID | P | Device | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RESUME-01 | P1 | mouse | While a contest is waiting, the lobby shows the resume banner above the event dock: "Saved at N seconds. Your result is waiting." with **Resume contest** ([Summary](../watch/resume-a-contest.md#summary)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, reload.<br>2. Read the lobby. | The banner, worded as quoted, sits above **CHOOSE YOUR EVENT**, with N the saved second rounded down. | pass (scripted pass 2026-09-24): "Saved at 1 seconds. Your result is waiting. Resume contest". |
| RESUME-02 | P1 | mouse | **Resume contest** loads the recording at the saved second, paused; the stage rebuilds, and the banner and event dock disappear ([Summary](../watch/resume-a-contest.md#summary); [Committing](../watch/resume-a-contest.md#committing)). | As RESUME-01, with the banner showing. | 1. Press **Resume contest**.<br>2. Wait 10 s and watch the progress bar. | **UNFOLDING THE ARENA…**, then the moment it was left, frozen. The button reads "Resume playback" and the bottom bar **PAUSED**. No banner and no event dock. The progress bar does not move. | pass (scripted pass 2026-09-24): it opened paused ("Resume playback", "CARDS TO COURT / PAUSED"), and the progress did not advance while paused. |
| RESUME-03 | P1 | mouse | A resumed contest opens at **1×** ([Committing](../watch/resume-a-contest.md#committing)). | Fresh profile, default choices. Lock an exhibition and set **2×**. | 1. During ROUND 1, click the logo.<br>2. Press **Resume contest** and read the speed button. | **1×**. | pass (scripted pass 2026-09-24): **Resume contest** opened at 1×. |
| RESUME-04 | P2 | mouse | **Resume playback** continues the resumed contest from where it was left ([The simple case](../watch/resume-a-contest.md#the-simple-case)). | As RESUME-02, after step 1. | 1. Press "Resume playback". | The same attempt carries on from the frozen frame; the bottom bar reads **● LIVE**. | — |
| RESUME-05 | P1 | storage | A contest becomes the one waiting to resume as soon as it is locked, at second 0 ([Starting](../watch/resume-a-contest.md#starting)). | Fresh profile, default choices, developer tools on localStorage. | 1. Press **Start showdown**.<br>2. As soon as the dialog starts closing, read `active`. | `active` is the new recording's ID with time 0. | pass (scripted pass 2026-09-24): locking set the waiting contest to `{id, time: 0}`. |
| RESUME-06 | P1 | storage | Clicking the logo writes the saved second ([Starting](../watch/resume-a-contest.md#starting)). | Fresh profile, default choices. Lock an exhibition and pause it during ROUND 2. | 1. Note the playback moment and `active.time`.<br>2. Click the logo and read `active.time` and the banner. | `active.time` now equals the paused moment, and the banner shows that second rounded down. | — |
| RESUME-07 | P1 | storage | The page going away, by reload or closing the tab, writes the saved second ([Starting](../watch/resume-a-contest.md#starting)). | Fresh profile, default choices. Lock an exhibition. | 1. During ROUND 1, reload.<br>2. Read `active.time`. | It matches the moment of the reload to within a frame, not just the last 2-second write. | pass (scripted pass 2026-09-24): a reload mid-playback kept the waiting contest with time 1.14 s. |
| RESUME-08 | P1 | storage | Loading another recording does not write the old contest's position; the new recording takes over the waiting slot with its own first position ([Starting](../watch/resume-a-contest.md#starting)). | Fresh profile, default choices. Lock an exhibition and let it play into ROUND 2. | 1. Note `active.time`.<br>2. Open History and **Replay** the basketball row; read `active` at once.<br>3. Read it again 3 s later. | Step 2: `active` still names the exhibition, with no new time written. Step 3: `active` names the basketball recording. The exhibition is never offered again. (B-05) | — |
| RESUME-09 | P2 | storage | The saved second never goes past the recording's end; a write made at or after the end of the last attempt clears the waiting contest instead ([Starting](../watch/resume-a-contest.md#starting)). | Fresh profile, default choices. Lock an exhibition and watch it to the end. | 1. As the result panel appears, while the finale still animates, click the logo.<br>2. Read `active` and the lobby. | `active` is null, and there is no banner. | — |
| RESUME-10 | P1 | mouse | The viewer can ignore the banner and start another contest, which replaces the waiting one with no confirmation; a waiting counted entry's points are then revealed ([Backing out at once](../watch/resume-a-contest.md#backing-out-at-once); [Edge cases](../watch/resume-a-contest.md#edge-cases)). | Fresh profile, default choices. Lock a counted entry and reload during ROUND 1, so the banner shows. | 1. **Set up showdown**, **Exhibition · no points**, **Start showdown**.<br>2. **Skip to result**, **Next showdown**.<br>3. Read the lobby and the chip. | No warning at step 1. Afterwards there is no banner, and the chip includes Doug's award for the counted entry that was never watched to the end: 3 plus that award in `ledger`. (B-05) | — |
| RESUME-11 | P2 | mouse + storage | A contest saved at second 0 plays by itself once the stage is ready, from the entrances, instead of opening paused ([Committing](../watch/resume-a-contest.md#committing); [Edge cases](../watch/resume-a-contest.md#edge-cases)). | Fresh profile, default choices. | 1. Press **Start showdown**; while **UNFOLDING THE ARENA…** still shows, reload.<br>2. Check that the banner reads "Saved at 0 seconds" and `active.time` is 0.<br>3. Press **Resume contest**. | The entrances play by themselves once the stage is ready: "Pause playback" and **● LIVE**, not **PAUSED**. | — |
| RESUME-12 | P1 | mouse | While a counted entry is waiting, its points are left out of the chip, Standings and member records, while the chip's **entries left** already counts it ([What "waiting" hides](../watch/resume-a-contest.md#what-waiting-hides)). | Fresh profile, default choices. Lock a counted entry and reload during ROUND 1. | 1. Read the chip.<br>2. Open Standings and Doug's member record. | The chip reads "3 CLUB POINTS" and "2 entries left". Standings: Doug 3, **Played** 1. The member record: 3 points and one contest. | — |
| RESUME-13 | P1 | mouse | A waiting contest is left out of History ([What "waiting" hides](../watch/resume-a-contest.md#what-waiting-hides)). | As RESUME-12. | 1. Open the footer's **History**. | Only the basketball row. No cornhole row. | — |
| RESUME-14 | P1 | mouse | An exhibition hides no points but still waits to resume, with the same "Your result is waiting." banner, and still hides from History ([What "waiting" hides](../watch/resume-a-contest.md#what-waiting-hides); [Edge cases](../watch/resume-a-contest.md#edge-cases)). | Fresh profile, default choices. Lock an exhibition and reload during ROUND 1. | 1. Read the banner.<br>2. Open History. | Record what happens (suspected bug). The document says the banner "Saved at N seconds. Your result is waiting." appears for an exhibition, which has no result to wait for, and History does not list it until it completes. (B-01) | — |
| RESUME-15 | P2 | mouse | The resumed recording's sport replaces the lobby's selected event ([Modifiers](../watch/resume-a-contest.md#modifiers)). | Fresh profile, default choices. Lock a Cornhole exhibition, click the logo during ROUND 1, then click **02 Football**. | 1. Press **Resume contest**.<br>2. Click the logo. | Step 1: **CORNHOLE / LIVE** and the cornhole court. Step 2: the lobby is on **01 Cornhole**, not Football. | — |
| RESUME-16 | P2 | mouse | The recording's own cards are loaded, whatever the lobby had selected ([Modifiers](../watch/resume-a-contest.md#modifiers)). | Fresh profile, default choices. Lock an exhibition (Dan's card first), click the logo, then in the dialog choose Doug's card as yours and Dan's for the opponent, and close. | 1. Press **Resume contest**.<br>2. Read the nameplates. | **DAN** on the left and **DOUG** on the right, as in the recording, not the lobby's new order. | — |
| RESUME-17 | P3 | small screen | The banner wraps on narrow screens ([Modifiers](../watch/resume-a-contest.md#modifiers)). | Banner showing as in RESUME-01. | 1. Set the width to 390 px. | The whole banner text and **Resume contest** are visible, wrapped inside the page. No horizontal scrolling. | — |
| RESUME-18 | P2 | two tabs | Another tab can replace or clear the waiting contest, and this tab's banner follows the save it re-reads ([Modifiers](../watch/resume-a-contest.md#modifiers); [Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | Fresh profile. Tab A shows the banner for an exhibition left at ROUND 1; tab B is on the lobby. | 1. In B, lock another exhibition.<br>2. Look at A.<br>3. In B, **Reset demo data**; look at A. | After step 1, A's banner reads "Saved at 0 seconds" for B's new contest. After step 3, A's banner is gone. A was not reloaded. | — |
| RESUME-19 | P2 | keyboard | Behind a dialog, the resumed contest stays paused or keeps playing, and Escape closes the dialog with the contest still loaded ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | As RESUME-02, after step 1. | 1. Open House rules; wait 5 s; press Escape.<br>2. Press "Resume playback", open House rules, wait 5 s, press Escape. | Step 1: still paused at the same moment. Step 2: it kept playing behind the dialog. The recording stays loaded throughout. | — |
| RESUME-20 | P2 | mouse | **Resume contest** can only be pressed once, because the banner disappears ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | Banner showing as in RESUME-01. | 1. Double-click **Resume contest**. | One load, with **UNFOLDING THE ARENA…** once, and no error. | — |
| RESUME-21 | P2 | mouse | The banner reappears whenever the lobby is shown, including after a reload ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | Banner showing as in RESUME-01. | 1. Switch to **Standings** and back to **Watch**.<br>2. Reload. | The banner, with the same second, both times. | — |
| RESUME-22 | P2 | mouse | After resuming, leaving by the logo writes the position again ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | As RESUME-04, after about 5 s of playing. | 1. Click the logo. | The banner shows a later second than before the resume. | — |
| RESUME-23 | P1 | mouse + storage | **Skip to result** completes a resumed contest and clears the waiting state ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | As RESUME-12, then **Resume contest**. | 1. Press "Skip to result".<br>2. Read `active`, the chip, then click the logo. | `active` is null, and the chip includes Doug's new award. No banner in the lobby. | — |
| RESUME-24 | P2 | two tabs | While a resumed contest plays, another tab's playback writes its own position, and the last write wins ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | Fresh profile. Tab A: a resumed exhibition, playing. Tab B: the lobby. | 1. In B, History, **Replay** the basketball row.<br>2. Read `active` twice, 3 s apart.<br>3. Pause B; wait 4 s; read `active`. | `active` switches between A's and B's recordings as each writes. With B paused, it names A's recording. | — |
| RESUME-25 | P2 | storage | If a position write fails during a resumed contest, the error box reads **Playback could not be saved. Keep this tab open and export your recording.** ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)). | As RESUME-04. | 1. Fill localStorage to its quota.<br>2. Wait 4 s. | The error box shows the message, and playback carries on. | — |
| RESUME-26 | P1 | storage | The waiting contest and its second are exported with **Export local save** ([Interactions with other systems](../watch/resume-a-contest.md#interactions-with-other-systems)). | Fresh profile, default choices. Lock a counted entry and reload during ROUND 1. | 1. Arena settings, **Export local save**.<br>2. Compare `clubhouse-save.json` with the save: `recordings`, `ledger` and `active`. | Record what happens (suspected bug). The document says the waiting contest is exported. [Contests and recordings](../foundations/contests-and-recordings.md#written-and-revealed) says the file leaves the waiting recording and its awards out, while `active` still names it. (B-11) | confirmed (scripted pass 2026-09-24, with a waiting replay of a counted entry): the file had 4 awards while the save had 6. |
| RESUME-27 | P2 | mouse | Resuming does not change the Watch sound switch ([Interactions with other systems](../watch/resume-a-contest.md#interactions-with-other-systems)). | Fresh profile, default choices. Lock an exhibition, turn Watch sound on, click the logo. | 1. Press **Resume contest**; read the speaker button and the footer.<br>2. Press "Resume playback" and listen. | The button is still named "Mute sound", and the footer shows sound on. Cues are heard. | — |
| RESUME-28 | P2 | storage | The banner is plain text with a labelled button, and it is not announced as a live region ([Interactions with other systems](../watch/resume-a-contest.md#interactions-with-other-systems)). | Banner showing as in RESUME-01. | 1. Read the banner in the accessibility tree. | Text plus a button named "Resume contest". No live-region attribute on the banner. | — |
| RESUME-29 | P1 | mouse | An abandoned replay of a finished contest becomes the waiting contest, and its already-seen points are hidden again until it is finished ([Edge cases](../watch/resume-a-contest.md#edge-cases)). | Fresh profile, default choices. Lock a counted entry, **Skip to result**, **Replay same recording**. | 1. During the replay's entrances, click the logo.<br>2. Read the banner and the chip. | Record what happens (suspected bug). The document says the banner offers the finished contest, and the chip leaves its points out. (B-01) | confirmed (scripted pass 2026-09-24): "Saved at 0 seconds. Your result is waiting." with the chip back at 3. |
| RESUME-30 | P3 | storage | The banner rounds the saved second down, but resuming uses the exact saved moment ([Edge cases](../watch/resume-a-contest.md#edge-cases)). | As RESUME-01. | 1. Read `active.time` and the banner.<br>2. **Resume contest** and compare the paused frame with the moment of the reload. | The banner shows the whole seconds of `active.time`, for example 1.14 as "1". The resumed frame is the reload's moment, not the whole second. | pass (scripted pass 2026-09-24, banner only): 1.14 s showed as "Saved at 1 seconds" (the plural slip is B-36). |

Not checkable by hand:

- Whether replacing a waiting counted entry, without warning, should ask for confirmation is a product call ([Open questions and verification](../watch/resume-a-contest.md#open-questions-and-verification); B-05).
- Whether the banner should appear for exhibitions and finished replays at all is a product question behind RESUME-14 and RESUME-29 ([Open questions and verification](../watch/resume-a-contest.md#open-questions-and-verification); B-01).
- A waiting contest whose recording is no longer in this browser's save, where **Resume contest** does nothing, cannot be produced without hand-editing the checksummed save ([Cancel and interrupt](../watch/resume-a-contest.md#cancel-and-interrupt)).
- What shows when a waiting recording uses an installed character that is no longer in the character library is not stated. The document says only that the character is needed ([Interactions with other systems](../watch/resume-a-contest.md#interactions-with-other-systems)).
- `read_arena` not reporting a waiting contest needs a browser that ships WebMCP ([Interactions with other systems](../watch/resume-a-contest.md#interactions-with-other-systems)).
